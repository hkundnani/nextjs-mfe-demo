{
  "name": "nextjs-mfe-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.1.0",
    "wsrun": "^5.2.0"
  },
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "start": "concurrently \"wsrun --parallel dev\""
  }
}
